// Stub for Prisma client - will be replaced when database is set up
export const prisma = {
  personalContext: {
    findUnique: async () => null,
    upsert: async (data: unknown) => data,
    delete: async () => ({}),
  },
} as any;
